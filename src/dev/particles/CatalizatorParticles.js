let GoldParticle = Particles.registerParticleType({
  texture: "nitor",
  size: [0.25, 2],
  lifetime: [60, 60],

  color: [0.77, 0.67, 0, 0.4],

  render: 0,
})

let EmeraldParticle = Particles.registerParticleType({
  texture: "nitor",
  size: [0.25, 2],
  lifetime: [60, 60],

  color: [0, 0.69, 0.17, 0.4],

  render: 0,
})

let IronParticle = Particles.registerParticleType({
  texture: "nitor",
  size: [0.25, 2],
  lifetime: [60, 60],

  color: [0.7, 0.7, 0.7, 0.4],

  render: 0,
})

let LypisParticle = Particles.registerParticleType({
  texture: "nitor",
  size: [0.25, 2],
  lifetime: [60, 60],

  color: [0.11, 0.31, 0.74, 0.4],

  render: 0,
})

let DiamondParticle = Particles.registerParticleType({
  texture: "nitor",
  size: [0.25, 2],
  lifetime: [60, 60],

  color: [0.12, 0.74, 0.70, 0.4],

  render: 0,
})

